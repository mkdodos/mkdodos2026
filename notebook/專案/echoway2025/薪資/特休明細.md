在薪資表單的按鈕,按下後顯示明細(modal),
modal 中放 Dayoff 組件,傳年月過去

import Dayoff from "./components/dayoff";

Dayoff
import { printTable } from "../../../../components/Pdf";

  <Button onClick={() => printTable(state.data, schema.columns)}>

        PDF

      </Button>


在明細中按下PDF鈕,產生PDF